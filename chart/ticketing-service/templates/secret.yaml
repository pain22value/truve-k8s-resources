apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Release.Name }}-ticketing-secrets
  namespace: truve-ticketing-service
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: truve-secret-store
    kind: ClusterSecretStore
  target:
    name: {{ .Release.Name }}-ticketing-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: prod/ticketing-service
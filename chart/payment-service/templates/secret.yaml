apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-payment-secrets
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Release.Name }}-queue-secrets
  namespace: truve-queue-service
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: truve-secret-store
    kind: ClusterSecretStore
  target:
    name: {{ .Release.Name }}-queue-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: prod/queue-service
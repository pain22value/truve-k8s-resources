apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: queue-service
  labels:
    app: queue-service
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: queue-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scrapeTimeout: 10s
